"""Metric summary display cards."""

from typing import Any, Dict


def render_metric_card(title: str, value: Any, delta: str | None = None) -> None:
    """Render a KPI metric card."""
    raise NotImplementedError("Metric card rendering not yet implemented.")
